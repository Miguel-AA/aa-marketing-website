<section class="aa-services2-breakout" id="services">
  <div class="aa-services2">
    <div class="aa-services2__glow aa-services2__glow--left" aria-hidden="true"></div>
    <div class="aa-services2__glow aa-services2__glow--right" aria-hidden="true"></div>
    <div class="aa-services2__lines" aria-hidden="true"></div>
    <div class="aa-services2__dust" aria-hidden="true"></div>

    <div class="aa-services2__inner">

      <!-- Header -->
      <div class="aa-services2__head">
        <div class="aa-kicker2">Our Services</div>

        <h2 class="aa-services2__title">
          High-performance marketing<br>
          built for sports brands.
        </h2>

        <p class="aa-services2__sub">
          Strategy-first execution that turns attention into growth. Content, social, and campaigns engineered to win.
        </p>
      </div>

      <!-- Grid -->
      <div class="aa-services2__grid" data-aa-accordion="single">

        <!-- 1 -->
        <article class="aa-acc" data-aa-acc>
          <button class="aa-acc__btn" type="button" aria-expanded="false">
            <span class="aa-acc__left">
              <span class="aa-acc__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M4 19V5m0 14h16" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                  <path d="M7 14l3-3 3 2 5-6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
              <span class="aa-acc__copy">
                <span class="aa-acc__title">Social Strategy</span>
                <span class="aa-acc__meta">Roadmap + cadence</span>
              </span>
            </span>
            <span class="aa-acc__chev" aria-hidden="true">+</span>
          </button>

          <div class="aa-acc__panel" role="region" aria-hidden="true">
            <div class="aa-acc__panelInner">
              <p class="aa-acc__text">
                Positioning, channel strategy, calendars, and growth systems designed for measurable reach and engagement.
              </p>
              <ul class="aa-acc__list">
                <li>Brand voice &amp; content pillars</li>
                <li>Content calendar &amp; cadence</li>
                <li>Analytics + optimization loop</li>
              </ul>
            </div>
          </div>
        </article>

        <!-- 2 -->
        <article class="aa-acc" data-aa-acc>
          <button class="aa-acc__btn" type="button" aria-expanded="false">
            <span class="aa-acc__left">
              <span class="aa-acc__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M7 7h10M7 12h10M7 17h6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                  <path d="M5 4h14a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H9l-4 2v-2H5a2 2 0 0 1-2-2V6a2 2 0 0 1 2-2Z"
                    stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
                </svg>
              </span>
              <span class="aa-acc__copy">
                <span class="aa-acc__title">Content Production</span>
                <span class="aa-acc__meta">Creative built for speed</span>
              </span>
            </span>
            <span class="aa-acc__chev" aria-hidden="true">+</span>
          </button>

          <div class="aa-acc__panel" role="region" aria-hidden="true">
            <div class="aa-acc__panelInner">
              <p class="aa-acc__text">
                Premium creative built for speed: short-form, campaign assets, and social-first storytelling that converts.
              </p>
              <ul class="aa-acc__list">
                <li>Short-form &amp; reels</li>
                <li>Design systems &amp; templates</li>
                <li>Copywriting for conversion</li>
              </ul>
            </div>
          </div>
        </article>

        <!-- 3 -->
        <article class="aa-acc" data-aa-acc>
          <button class="aa-acc__btn" type="button" aria-expanded="false">
            <span class="aa-acc__left">
              <span class="aa-acc__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M12 3l8 4v6c0 5-3.5 9-8 8-4.5 1-8-3-8-8V7l8-4Z"
                    stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
                  <path d="M9 12l2 2 4-5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </span>
              <span class="aa-acc__copy">
                <span class="aa-acc__title">Influencer &amp; Talent</span>
                <span class="aa-acc__meta">Creator-led campaigns</span>
              </span>
            </span>
            <span class="aa-acc__chev" aria-hidden="true">+</span>
          </button>

          <div class="aa-acc__panel" role="region" aria-hidden="true">
            <div class="aa-acc__panelInner">
              <p class="aa-acc__text">
                Partnerships and talent activations that feel authentic, aligned, and performance-driven.
              </p>
              <ul class="aa-acc__list">
                <li>Talent sourcing &amp; outreach</li>
                <li>Deal structure &amp; deliverables</li>
                <li>Campaign reporting</li>
              </ul>
            </div>
          </div>
        </article>

        <!-- 4 -->
        <article class="aa-acc" data-aa-acc>
          <button class="aa-acc__btn" type="button" aria-expanded="false">
            <span class="aa-acc__left">
              <span class="aa-acc__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M4 8h16M6 8V6a2 2 0 0 1 2-2h8a2 2 0 0 1 2 2v2" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                  <path d="M6 8v12a2 2 0 0 0 2 2h8a2 2 0 0 0 2-2V8" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
                  <path d="M9 12h6M9 16h6" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                </svg>
              </span>
              <span class="aa-acc__copy">
                <span class="aa-acc__title">Brand &amp; Campaigns</span>
                <span class="aa-acc__meta">Launch systems</span>
              </span>
            </span>
            <span class="aa-acc__chev" aria-hidden="true">+</span>
          </button>

          <div class="aa-acc__panel" role="region" aria-hidden="true">
            <div class="aa-acc__panelInner">
              <p class="aa-acc__text">
                Launches and campaigns with premium creative direction, structured messaging, and clear KPIs.
              </p>
              <ul class="aa-acc__list">
                <li>Creative direction</li>
                <li>Messaging framework</li>
                <li>Launch planning</li>
              </ul>
            </div>
          </div>
        </article>

        <!-- 5 -->
        <article class="aa-acc aa-acc--featured" data-aa-acc>
          <button class="aa-acc__btn" type="button" aria-expanded="false">
            <span class="aa-acc__left">
              <span class="aa-acc__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M12 2v4M12 18v4M4 12H2M22 12h-2" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                  <path d="M7 7l-1.5-1.5M18.5 18.5L17 17M17 7l1.5-1.5M5.5 18.5L7 17"
                    stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                  <path d="M12 8a4 4 0 1 1 0 8a4 4 0 0 1 0-8Z" stroke="currentColor" stroke-width="1.8"/>
                </svg>
              </span>
              <span class="aa-acc__copy">
                <span class="aa-acc__title">Performance Growth</span>
                <span class="aa-acc__meta">Paid + organic scale</span>
              </span>
            </span>
            <span class="aa-acc__chev" aria-hidden="true">+</span>
          </button>

          <div class="aa-acc__panel" role="region" aria-hidden="true">
            <div class="aa-acc__panelInner">
              <p class="aa-acc__text">
                Paid + organic systems that scale. We build repeatable pipelines and optimize weekly for outcomes.
              </p>
              <ul class="aa-acc__list">
                <li>Paid social &amp; retargeting</li>
                <li>Landing pages &amp; funnels</li>
                <li>KPI dashboard + testing</li>
              </ul>
            </div>
          </div>
        </article>

        <!-- 6 -->
        <article class="aa-acc" data-aa-acc>
          <button class="aa-acc__btn" type="button" aria-expanded="false">
            <span class="aa-acc__left">
              <span class="aa-acc__icon" aria-hidden="true">
                <svg viewBox="0 0 24 24" fill="none">
                  <path d="M4 20V6a2 2 0 0 1 2-2h12a2 2 0 0 1 2 2v14" stroke="currentColor" stroke-width="1.8" stroke-linejoin="round"/>
                  <path d="M8 10h8M8 14h8" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                  <path d="M6 20h12" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/>
                </svg>
              </span>
              <span class="aa-acc__copy">
                <span class="aa-acc__title">Website &amp; Funnels</span>
                <span class="aa-acc__meta">Conversion paths</span>
              </span>
            </span>
            <span class="aa-acc__chev" aria-hidden="true">+</span>
          </button>

          <div class="aa-acc__panel" role="region" aria-hidden="true">
            <div class="aa-acc__panelInner">
              <p class="aa-acc__text">
                Premium landing pages and funnel experiences aligned to your brand and built to convert.
              </p>
              <ul class="aa-acc__list">
                <li>Hero + section systems</li>
                <li>Conversion-first layout</li>
                <li>Speed + SEO basics</li>
              </ul>
            </div>
          </div>
        </article>

      </div>

      <!-- Bottom CTA -->
      <div class="aa-services2__foot">
        <div class="aa-services2__note">
          Need a custom package? We'll build a plan around your goals.
        </div>
        <a class="aa-services2__cta" href="#contact">Request a consultation</a>
      </div>

    </div>
  </div>

  <style>
    @import url('https://fonts.googleapis.com/css2?family=Inter:wght@500;600;700;800;900&display=swap');

    .aa-services2-breakout{
      position: relative;
      left: 50%;
      right: 50%;
      margin-left: -50vw;
      margin-right: -50vw;
      width: 100vw;
    }

    #services.aa-services2-breakout .aa-services2{
      position: relative;
      padding: 140px 0 132px; /* NO TOCADO */
      background: transparent !important;
      overflow: hidden;
      isolation: isolate;
    }

    #services .aa-services2__glow{
      position:absolute;
      z-index:0;
      pointer-events:none;
      border-radius:999px;
      filter: blur(10px);
    }

    #services .aa-services2__glow--left{
      width: 520px;
      height: 520px;
      left: -180px;
      top: 160px;
      background: radial-gradient(closest-side, rgba(255,196,0,.12), transparent 72%);
      opacity:.65;
    }

    #services .aa-services2__glow--right{
      width: 620px;
      height: 620px;
      right: -220px;
      bottom: -100px;
      background: radial-gradient(closest-side, rgba(245,247,250,.08), transparent 72%);
      opacity:.56;
    }

    #services .aa-services2__lines{
      position:absolute;
      inset:0;
      z-index:0;
      pointer-events:none;
      opacity:.11;
      background: repeating-linear-gradient(
        135deg,
        rgba(255,196,0,.16) 0px,
        rgba(255,196,0,.16) 1px,
        transparent 1px,
        transparent 26px
      );
      mask-image: radial-gradient(900px 650px at 65% 35%, black 0%, black 50%, transparent 78%);
      -webkit-mask-image: radial-gradient(900px 650px at 65% 35%, black 0%, black 50%, transparent 78%);
      mix-blend-mode: screen;
    }

    #services .aa-services2__dust{
      position:absolute;
      inset:0;
      z-index:0;
      pointer-events:none;
      opacity:.14;
      background-image:
        radial-gradient(circle at 16% 20%, rgba(255,196,0,.18) 0 1px, transparent 2px),
        radial-gradient(circle at 28% 70%, rgba(245,247,250,.16) 0 1px, transparent 2px),
        radial-gradient(circle at 52% 42%, rgba(255,196,0,.12) 0 1px, transparent 2px),
        radial-gradient(circle at 72% 26%, rgba(245,247,250,.14) 0 1px, transparent 2px),
        radial-gradient(circle at 84% 62%, rgba(255,196,0,.11) 0 1px, transparent 2px);
      background-size: 560px 560px;
      mix-blend-mode: screen;
    }

    #services .aa-services2__inner{
      position: relative;
      z-index: 2;
      max-width: 1320px;
      margin: 0 auto;
      padding: 0 22px;
    }

    #services .aa-services2__head{
      max-width: 900px;
      margin: 0 auto 42px;
      text-align: center;
    }

    #services .aa-kicker2{
      display:inline-flex;
      align-items:center;
      gap:10px;
      font-family: Inter, system-ui, sans-serif;
      font-weight: 900;
      font-size: 12px;
      letter-spacing: .26em;
      text-transform: uppercase;
      color: rgba(245,247,250,.94);
      text-shadow: 0 12px 34px rgba(0,0,0,.35);
    }

    #services .aa-kicker2::before{
      content:"";
      width: 28px;
      height: 2px;
      border-radius: 2px;
      background: #FFC400;
      box-shadow: 0 10px 26px rgba(255,196,0,.24);
    }

    #services .aa-services2__title{
      margin: 16px 0 0;
      font-family: Inter, system-ui, sans-serif;
      font-weight: 900;
      font-size: clamp(38px, 5vw, 72px);
      line-height: .96;
      letter-spacing: -0.05em;
      text-transform: uppercase;
      color: #F5F7FA;
      text-shadow:
        0 18px 54px rgba(0,0,0,.52),
        0 0 34px rgba(6,26,58,.38);
    }

    #services .aa-services2__sub{
      margin: 18px auto 0;
      max-width: 720px;
      font-family: Inter, system-ui, sans-serif;
      font-size: 15px;
      line-height: 1.8;
      color: rgba(245,247,250,.82);
      font-weight: 700;
      text-shadow: 0 12px 34px rgba(0,0,0,.22);
    }

    #services .aa-services2__grid{
      display:grid;
      grid-template-columns: repeat(3, minmax(0, 1fr));
      gap: 18px 22px;
      align-items: start;
    }

    #services .aa-acc{
      position: relative;
      border-radius: 22px;
      border: 1px solid rgba(255,196,0,.16);
      background:
        linear-gradient(145deg, rgba(245,247,250,.075), rgba(245,247,250,.02)),
        rgba(6,26,58,.36);
      box-shadow:
        0 28px 74px rgba(0,0,0,.28),
        inset 0 1px 0 rgba(255,255,255,.05);
      overflow: hidden;
      transition: transform .22s ease, border-color .22s ease, box-shadow .22s ease, background .22s ease;
      backdrop-filter: blur(10px);
      -webkit-backdrop-filter: blur(10px);
    }

    #services .aa-acc::before{
      content:"";
      position:absolute;
      inset:-35% -35% auto -35%;
      height: 190px;
      background: radial-gradient(closest-side, rgba(255,255,255,.10), rgba(255,255,255,0));
      opacity:.15;
      pointer-events:none;
    }

    #services .aa-acc::after{
      content:"";
      position:absolute;
      inset:auto -35% -70% -35%;
      height: 260px;
      background: radial-gradient(closest-side, rgba(255,196,0,.12), rgba(255,196,0,0));
      opacity:.18;
      pointer-events:none;
    }

    #services .aa-acc:hover{
      transform: translateY(-4px);
      border-color: rgba(255,196,0,.26);
      box-shadow:
        0 34px 88px rgba(0,0,0,.34),
        inset 0 1px 0 rgba(255,255,255,.06);
    }

    #services .aa-acc--featured{
      border-color: rgba(255,196,0,.34);
      background:
        linear-gradient(145deg, rgba(255,196,0,.08), rgba(245,247,250,.025)),
        rgba(6,26,58,.42);
      box-shadow:
        0 34px 88px rgba(0,0,0,.36),
        0 0 0 1px rgba(255,196,0,.08),
        inset 0 1px 0 rgba(255,255,255,.07);
    }

    #services .aa-acc--featured::after{
      opacity:.32;
      background: radial-gradient(closest-side, rgba(255,196,0,.22), rgba(255,196,0,0));
    }

    #services .aa-acc__btn{
      width:100%;
      border:0;
      background: transparent;
      padding: 18px 18px;
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 16px;
      text-align:left;
      cursor:pointer;
      color: inherit;
    }

    #services .aa-acc__left{
      display:flex;
      align-items:center;
      gap: 14px;
      min-width: 0;
      flex: 1;
    }

    #services .aa-acc__icon{
      width: 44px;
      height: 44px;
      border-radius: 14px;
      flex: 0 0 auto;
      display:flex;
      align-items:center;
      justify-content:center;
      color: rgba(245,247,250,.90);
      border: 1px solid rgba(255,196,0,.24);
      background:
        linear-gradient(145deg, rgba(255,255,255,.08), rgba(255,255,255,.02)),
        rgba(245,247,250,.04);
      box-shadow:
        inset 0 1px 0 rgba(255,255,255,.05),
        0 16px 38px rgba(0,0,0,.18);
    }

    #services .aa-acc__icon svg{
      width: 21px;
      height: 21px;
      display:block;
    }

    #services .aa-acc__copy{
      min-width: 0;
      display:flex;
      flex-direction:column;
      gap: 4px;
    }

    #services .aa-acc__title{
      font-family: Inter, system-ui, sans-serif;
      font-size: 14px;
      font-weight: 900;
      line-height: 1.15;
      letter-spacing: -0.02em;
      text-transform: uppercase;
      color: rgba(245,247,250,.96);
      text-shadow: 0 8px 22px rgba(0,0,0,.20);
    }

    #services .aa-acc__meta{
      font-family: Inter, system-ui, sans-serif;
      font-size: 11px;
      font-weight: 900;
      letter-spacing: .14em;
      text-transform: uppercase;
      color: #FFC400;
    }

    #services .aa-acc__chev{
      width: 40px;
      height: 40px;
      border-radius: 14px;
      flex: 0 0 auto;
      display:flex;
      align-items:center;
      justify-content:center;
      border: 1px solid rgba(255,196,0,.22);
      background: rgba(245,247,250,.04);
      color: rgba(245,247,250,.92);
      font-family: Inter, system-ui, sans-serif;
      font-weight: 900;
      font-size: 24px;
      line-height: 1;
      transition: transform .2s ease, background .2s ease, border-color .2s ease;
    }

    #services .aa-acc.is-open .aa-acc__chev{
      transform: rotate(45deg);
      background: rgba(255,196,0,.10);
      border-color: rgba(255,196,0,.36);
      color: #FFC400;
    }

    #services .aa-acc__panel{
      max-height: 0;
      overflow: hidden;
      opacity: 0;
      transition: max-height .28s ease, opacity .22s ease;
    }

    #services .aa-acc.is-open .aa-acc__panel{
      opacity: 1;
    }

    #services .aa-acc__panelInner{
      padding: 0 18px 18px 18px;
      border-top: 1px solid rgba(245,247,250,.08);
      margin-top: 2px;
    }

    #services .aa-acc__text{
      margin: 16px 0 0;
      font-family: Inter, system-ui, sans-serif;
      font-size: 14px;
      line-height: 1.72;
      color: rgba(245,247,250,.80);
      font-weight: 650;
    }

    #services .aa-acc__list{
      list-style: none;
      margin: 14px 0 0;
      padding: 0;
      display:grid;
      gap: 10px;
    }

    #services .aa-acc__list li{
      position: relative;
      padding-left: 18px;
      font-family: Inter, system-ui, sans-serif;
      font-size: 13px;
      line-height: 1.55;
      color: rgba(245,247,250,.88);
      font-weight: 700;
    }

    #services .aa-acc__list li::before{
      content:"";
      position:absolute;
      left:0;
      top:.55em;
      width: 8px;
      height: 8px;
      border-radius: 999px;
      background: #FFC400;
      box-shadow: 0 0 0 4px rgba(255,196,0,.08);
      transform: translateY(-50%);
    }

    #services .aa-services2__foot{
      margin-top: 26px;
      padding-top: 20px;
      border-top: 1px solid rgba(245,247,250,.10);
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 18px;
      flex-wrap: wrap;
    }

    #services .aa-services2__note{
      font-family: Inter, system-ui, sans-serif;
      font-size: 14px;
      line-height: 1.6;
      color: rgba(245,247,250,.82);
      font-weight: 700;
      max-width: 66ch;
    }

    #services .aa-services2__cta{
      display:inline-flex;
      align-items:center;
      justify-content:center;
      min-height: 48px;
      padding: 0 22px;
      text-decoration: none;
      border-radius: 16px;
      background: linear-gradient(135deg, #FFC400 0%, #FFD84D 100%);
      border: 1px solid rgba(255,196,0,.56);
      color: #061A3A;
      font-family: Inter, system-ui, sans-serif;
      font-size: 12px;
      font-weight: 900;
      letter-spacing: .16em;
      text-transform: uppercase;
      box-shadow:
        0 18px 45px rgba(0,0,0,.24),
        0 12px 34px rgba(255,196,0,.18);
      transition: transform .18s ease, box-shadow .18s ease, opacity .18s ease;
      white-space: nowrap;
    }

    #services .aa-services2__cta:hover{
      transform: translateY(-2px);
      opacity: .98;
      box-shadow:
        0 24px 60px rgba(0,0,0,.32),
        0 16px 40px rgba(255,196,0,.22);
    }

    #services .aa-services2__cta:focus,
    #services .aa-services2__cta:focus-visible,
    #services .aa-acc__btn:focus,
    #services .aa-acc__btn:focus-visible{
      outline: none !important;
    }

    #services .aa-services2__cta:focus-visible{
      box-shadow:
        0 0 0 3px rgba(255,196,0,.52),
        0 22px 60px rgba(0,0,0,.28) !important;
    }

    @media (max-width: 1080px){
      #services .aa-services2__grid{
        grid-template-columns: repeat(2, minmax(0, 1fr));
      }
    }

    @media (max-width: 780px){
      #services .aa-services2__grid{
        grid-template-columns: 1fr;
      }

      #services .aa-services2__head{
        margin-bottom: 28px;
      }

      #services .aa-services2__foot{
        align-items: stretch;
      }

      #services .aa-services2__cta{
        width: 100%;
      }
    }

    @media (max-width: 640px){
      #services .aa-services2__title{
        font-size: clamp(30px, 8vw, 42px);
      }

      #services .aa-services2__sub{
        font-size: 14px;
        line-height: 1.7;
      }

      #services .aa-acc__btn{
        padding: 16px;
      }

      #services .aa-acc__panelInner{
        padding: 0 16px 16px 16px;
      }

      #services .aa-acc__left{
        gap: 12px;
      }

      #services .aa-acc__icon{
        width: 42px;
        height: 42px;
      }
    }
    /* =========================================================
   SERVICES BACKGROUND UPGRADE — DO NOT TOUCH PADDING
   ========================================================= */

/* Keep original padding untouched */
#services.aa-services2-breakout .aa-services2{
  background:
    radial-gradient(900px 520px at 12% 18%, rgba(255,196,0,.16), transparent 62%),
    radial-gradient(720px 460px at 88% 22%, rgba(245,247,250,.09), transparent 64%),
    radial-gradient(760px 520px at 50% 88%, rgba(255,196,0,.10), transparent 68%),
    linear-gradient(180deg, rgba(6,26,58,.98) 0%, rgba(4,18,42,.96) 45%, rgba(3,13,31,.98) 100%) !important;
  isolation: isolate;
}

/* Deep premium vignette */
#services.aa-services2-breakout .aa-services2::before{
  content:"";
  position:absolute;
  inset:0;
  z-index:0;
  pointer-events:none;
  background:
    radial-gradient(900px 620px at 50% 42%, rgba(255,255,255,.045), transparent 65%),
    linear-gradient(90deg, rgba(0,0,0,.20) 0%, transparent 18%, transparent 82%, rgba(0,0,0,.22) 100%),
    linear-gradient(180deg, rgba(0,0,0,.16) 0%, transparent 32%, rgba(0,0,0,.28) 100%);
}

/* Big abstract brand shapes */
#services.aa-services2-breakout .aa-services2::after{
  content:"";
  position:absolute;
  z-index:0;
  pointer-events:none;
  width: 980px;
  height: 980px;
  right: -360px;
  top: -260px;
  border-radius: 999px;
  background:
    conic-gradient(
      from 210deg,
      rgba(255,196,0,0) 0deg,
      rgba(255,196,0,.16) 48deg,
      rgba(255,255,255,.06) 92deg,
      rgba(255,196,0,0) 150deg,
      rgba(255,196,0,0) 360deg
    );
  filter: blur(8px);
  opacity: .72;
  transform: rotate(-8deg);
}

/* Make inner content stay above the new background */
#services .aa-services2__inner{
  position: relative;
  z-index: 4;
}

/* Add a subtle horizontal light stage behind the cards */
#services .aa-services2__grid{
  position: relative;
}

#services .aa-services2__grid::before{
  content:"";
  position:absolute;
  left: 50%;
  top: 50%;
  width: min(1080px, 92vw);
  height: 360px;
  transform: translate(-50%, -48%);
  border-radius: 999px;
  background:
    radial-gradient(closest-side, rgba(245,247,250,.075), transparent 72%);
  filter: blur(4px);
  opacity: .9;
  pointer-events:none;
  z-index: -1;
}

/* Stronger diagonal line texture */
#services.aa-services2-breakout .aa-services2__lines{
  opacity: .18;
  background:
    repeating-linear-gradient(
      135deg,
      rgba(255,196,0,.18) 0px,
      rgba(255,196,0,.18) 1px,
      transparent 1px,
      transparent 24px
    ),
    repeating-linear-gradient(
      45deg,
      rgba(245,247,250,.045) 0px,
      rgba(245,247,250,.045) 1px,
      transparent 1px,
      transparent 42px
    );
  mask-image:
    radial-gradient(900px 650px at 62% 34%, black 0%, black 54%, transparent 78%);
  -webkit-mask-image:
    radial-gradient(900px 650px at 62% 34%, black 0%, black 54%, transparent 78%);
}

/* Add premium grid texture */
#services .aa-services2__dust{
  opacity: .18;
  background-image:
    radial-gradient(circle at 12% 24%, rgba(255,196,0,.24) 0 1px, transparent 2px),
    radial-gradient(circle at 28% 68%, rgba(245,247,250,.18) 0 1px, transparent 2px),
    radial-gradient(circle at 48% 34%, rgba(255,196,0,.14) 0 1px, transparent 2px),
    radial-gradient(circle at 68% 72%, rgba(245,247,250,.16) 0 1px, transparent 2px),
    radial-gradient(circle at 86% 28%, rgba(255,196,0,.16) 0 1px, transparent 2px);
  background-size: 520px 520px;
  mix-blend-mode: screen;
}

/* Give the cards more depth against the richer background */
#services .aa-acc{
  border-color: rgba(255,196,0,.20);
  background:
    linear-gradient(145deg, rgba(245,247,250,.09), rgba(245,247,250,.025) 44%),
    rgba(6,26,58,.64);
  box-shadow:
    0 30px 80px rgba(0,0,0,.34),
    inset 0 1px 0 rgba(255,255,255,.07);
}

/* Stronger featured card */
#services .aa-acc--featured{
  border-color: rgba(255,196,0,.48);
  background:
    linear-gradient(145deg, rgba(255,196,0,.20), rgba(245,247,250,.04) 48%),
    rgba(6,26,58,.68);
  box-shadow:
    0 38px 96px rgba(0,0,0,.42),
    0 0 0 1px rgba(255,196,0,.10),
    0 0 80px rgba(255,196,0,.16),
    inset 0 1px 0 rgba(255,255,255,.08);
}

/* Bigger glow on hover/open */
#services .aa-acc:hover{
  border-color: rgba(255,196,0,.36);
  box-shadow:
    0 38px 94px rgba(0,0,0,.42),
    0 0 70px rgba(255,196,0,.12),
    inset 0 1px 0 rgba(255,255,255,.08);
}

#services .aa-acc.is-open{
  border-color: rgba(255,196,0,.66) !important;
  box-shadow:
    0 42px 110px rgba(0,0,0,.48),
    0 0 0 1px rgba(255,196,0,.22),
    0 0 120px rgba(255,196,0,.26) !important;
}

/* Header feels less floating/plain */
#services .aa-services2__head{
  position: relative;
}

#services .aa-services2__head::after{
  content:"";
  position:absolute;
  left:50%;
  bottom:-22px;
  width: min(560px, 80vw);
  height: 1px;
  transform: translateX(-50%);
  background: linear-gradient(90deg, transparent, rgba(255,196,0,.72), transparent);
  box-shadow: 0 0 34px rgba(255,196,0,.22);
}

/* Title needs more visual weight */
#services .aa-services2__title{
  text-shadow:
    0 22px 64px rgba(0,0,0,.58),
    0 0 40px rgba(6,26,58,.48);
}

#services .aa-services2__sub{
  color: rgba(245,247,250,.84);
  font-weight: 750;
}
  </style>

  <script>
    (function(){
      const scope = document.getElementById('services');
      if(!scope) return;

      const accs = Array.from(scope.querySelectorAll('[data-aa-acc]'));
      const singleMode = scope.querySelector('[data-aa-accordion="single"]');

      function closeAcc(acc){
        const btn = acc.querySelector('.aa-acc__btn');
        const panel = acc.querySelector('.aa-acc__panel');
        if(!btn || !panel) return;
        acc.classList.remove('is-open');
        btn.setAttribute('aria-expanded', 'false');
        panel.setAttribute('aria-hidden', 'true');
        panel.style.maxHeight = '0px';
      }

      function openAcc(acc){
        const btn = acc.querySelector('.aa-acc__btn');
        const panel = acc.querySelector('.aa-acc__panel');
        if(!btn || !panel) return;
        acc.classList.add('is-open');
        btn.setAttribute('aria-expanded', 'true');
        panel.setAttribute('aria-hidden', 'false');
        panel.style.maxHeight = panel.scrollHeight + 'px';
      }

      accs.forEach((acc) => {
        const btn = acc.querySelector('.aa-acc__btn');
        const panel = acc.querySelector('.aa-acc__panel');
        if(!btn || !panel) return;

        closeAcc(acc);

        btn.addEventListener('click', function(){
          const isOpen = acc.classList.contains('is-open');

          if(singleMode){
            accs.forEach(item => {
              if(item !== acc) closeAcc(item);
            });
          }

          if(isOpen){
            closeAcc(acc);
          } else {
            openAcc(acc);
          }
        });
      });

      window.addEventListener('resize', function(){
        accs.forEach((acc) => {
          if(acc.classList.contains('is-open')){
            const panel = acc.querySelector('.aa-acc__panel');
            if(panel){
              panel.style.maxHeight = panel.scrollHeight + 'px';
            }
          }
        });
      }, { passive: true });
    })();
  </script>
</section>
